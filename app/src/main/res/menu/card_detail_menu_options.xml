<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/move_to_column_doing"
        android:title="Move to column doing"
        app:showAsAction="never"/>

    <item
        android:id="@+id/request_to_review"
        android:title="Request to review"
        app:showAsAction="never"/>

    <item
        android:id="@+id/back_to_column_todo"
        android:title="Back to column todo"
        app:showAsAction="never"/>

    <item
        android:id="@+id/cancel_to_review"
        android:title="Cancel request to review"
        app:showAsAction="never"/>

    <item
        android:id="@+id/approve_task"
        android:title="Approve task"
        app:showAsAction="never"/>

    <item
        android:id="@+id/reject_task"
        android:title="Reject task"
        app:showAsAction="never"/>
</menu>