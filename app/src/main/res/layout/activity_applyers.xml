<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.project.activeProject.applyers.ApplyersActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_applier"
        android:layout_width="0dp"
        android:layout_height="0dp"
        tools:listitem="@layout/item_row_applier_notification"
        android:layout_marginTop="@dimen/margin_size_small"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <include
        android:id="@+id/include_layout_applyers"
        layout="@layout/progress_loading"/>

    <include
        android:id="@+id/include_layout_info"
        layout="@layout/layout_info"/>

</androidx.constraintlayout.widget.ConstraintLayout>